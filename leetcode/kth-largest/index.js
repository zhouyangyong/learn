// 无序数组 找出第K大的数字
// 排序

// [1, 4, 9, 2, 6, 10] 

// function Kth(arr, K) {
//   return arr.sort((a, b) => {
//     return b-a
//   })[K - 1]
// }

function quickSort(arr) {
  let len = arr.length;
  let pivotIndex = Math.floor(len / 2);
  let piove = arr[pioveIndex];
}

function Kth(arr, K) {
  
}

console.log(Kth([1, 4, 9, 2, 6, 10], 2))