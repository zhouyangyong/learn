<block wx:for="{{products}}">
    <van-card 
        price="{{item.view}}" 
        desc="{{item.color}}"
        title="{{item.title}}"
        thumb="{{item.image}}"
        bindtap="{{item._id}}"
        bindtap="click"
    />
</block>